import { DynamicTabsDirective } from './dynamic-tabs.directive';

describe('DynamicTabsDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicTabsDirective();
    expect(directive).toBeTruthy();
  });
});
